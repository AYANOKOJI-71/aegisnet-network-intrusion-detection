# Deterministic fallback is the default; it never opens a live interface or generates traffic.
AEGISNET_STORE_MODE=memory
AEGISNET_ALLOWED_ORIGIN=http://localhost:5183

# Required only when AEGISNET_STORE_MODE=postgres.
AEGISNET_DATABASE_URL=postgresql://aegisnet:local-demo-only@localhost:5432/aegisnet
